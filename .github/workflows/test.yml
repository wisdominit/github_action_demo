name: Sample Workflow

on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        -   name: Welcome to my workflow
            uses: actions/checkout@v4

        -   name: List and Read Files
            run: |
               ls
               cat Readme.md

